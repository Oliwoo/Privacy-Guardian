<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/item_app_microphone_access"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/component_home_category_btn_bg"
    android:gravity="center"
    android:orientation="horizontal"
    android:padding="@dimen/spacing_medium">

    <ImageView
        android:id="@+id/item_app_microphone_access_icon"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_marginEnd="@dimen/spacing_medium"
        android:scaleType="fitCenter"
        app:srcCompat="@drawable/arrow_bottom"
        tools:ignore="ContentDescription" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/item_app_microphone_access_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="@font/inter_semibold"
            android:text="Title"
            android:textColor="@color/white"
            android:textSize="@dimen/font_size_text" />

        <TextView
            android:id="@+id/item_app_microphone_access_package"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="SubTitle"
            android:textColor="@color/white_50"
            android:textSize="@dimen/font_size_subtext" />

    </LinearLayout>

    <ImageView
        android:id="@+id/item_app_microphone_access_icon2"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:scaleType="fitCenter"
        app:srcCompat="@drawable/arrow_right"
        tools:ignore="ContentDescription" />

</LinearLayout>